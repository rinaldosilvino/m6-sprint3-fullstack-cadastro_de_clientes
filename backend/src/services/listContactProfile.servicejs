import contacts from "../database";

const listContactProfileService = (id) => {
  const index = contacts.findIndex((element) => element.uuid === id);

  const result = {...contacts[index]}
  delete result.password
  return result;
};

export default listContactProfileService;